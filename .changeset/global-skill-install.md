---
"@docker-doctor/cli": minor
---

Add `--global` flag to `docker-doctor install` — installs the agent skill into `~/.agents/skills` and each agent's global directory instead of the current project. Groundwork for the Docker Sandboxes kit.
